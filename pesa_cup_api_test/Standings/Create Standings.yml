info:
  name: Create Standings
  type: http
  seq: 3

http:
  method: POST
  url: "{{server_url}}/standings"
  body:
    type: json
    data: |-
      {
        "team": "Hello"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
