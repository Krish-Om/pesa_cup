info:
  name: CreateScorers
  type: http
  seq: 3

http:
  method: POST
  url: "{{server_url}}/scorers"
  body:
    type: json
    data: |-
      {
        "playerName": "Hambadullah",
        "teamId": 3,
        "tournamentId": 1,
        "goals": 0,
        "assists": 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
