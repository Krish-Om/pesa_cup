info:
  name: CreateScorers
  type: http
  seq: 3

http:
  method: POST
  url: "{{server_url}}/scorers"
  body:
    type: json
    data: |-
      {
        "playerName":"Hambadullah",
      "teamName":"Islam"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
