info:
  name: GetAllScorers
  type: http
  seq: 2

http:
  method: GET
  url: "{{server_url}}/scorers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
