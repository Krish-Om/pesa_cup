info:
  name: UpdateScorers
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{server_url}}/scorers/4"
  body:
    type: json
    data: |-
      {
        "tournamentId":1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
