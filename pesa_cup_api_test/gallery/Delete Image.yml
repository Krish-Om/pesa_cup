info:
  name: Delete Image
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{server_url}}/gallery/3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
