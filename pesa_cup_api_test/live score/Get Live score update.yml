info:
  name: Get Live score update
  type: http
  seq: 1

http:
  method: GET
  url: "{{server_url}}/fixtures/live-score"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
